{
  "name": "gander-gaming",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/gen-og-pages.js",
    "preview": "vite build && node scripts/gen-og-pages.js && firebase hosting:channel:deploy preview --expires 7d",
    "deploy": "vite build && node scripts/gen-og-pages.js && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "jspdf": "^4.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.2.13"
  }
}
